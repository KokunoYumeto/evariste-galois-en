# GPT-edition requirements for Codex — cumulative through P11

The diplomatic French layer remains immutable. Every critical repair requires a stable identifier, exact source coordinates, a proof or counterexample, witness provenance, and downstream dependency status.

P11 adds `W11-PE001`: the historical table of contents prints Arabic `1` for Picard's Introduction although the volume's logical foliation is roman `v-x`. The GPT/critical layer may render an explicit `1 → v` erratum, but it must preserve `1` in the diplomatic table and cite the primary-image plus W02 foliation certificate. Gutenberg's generated `1` is a control repetition and must not be described as independent corroboration.

P11 also adds `W11-U001`, the fused four-digit printer job number on the colophon. The source-derived image remains the only authorized representation until an independent copy or printer record supplies a secure character-by-character reading. A future critical layer must keep unresolved status visible rather than guessing.

P12 should carry all 36 proved source errors, four unresolved primary readings, 15 witness variants, and 21 deferred post-P13 audit tasks into the candidate handoff. The cumulative `POST_P13_BOUNDED_ERRATA_REPAIR_PROPAGATION_PROVENANCE_TASK.md` remains the governing program for proof repair, downstream propagation, and the requested online search for prior historical notice.

Detailed P11 requirements are in `P11_GPT_EDITION_NOTES_FOR_CODEX.md` and `P11_ERRATA_AND_READING_CERTIFICATES.md`.
